{
  "name": "spine-care-physiotherapy",
  "version": "1.0.0",
  "description": "Spine Care Physiotherapy Quetta",
  "main": "index.html",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -rf *.html assets components css js *.jpg *.jpeg *.webp *.pdf public/ 2>/dev/null || true",
    "start": "echo \"No start script needed\""
  },
  "engines": {
    "node": "20.x"
  }
}
